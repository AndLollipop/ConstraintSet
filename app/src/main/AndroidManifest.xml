<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lypop.com.lypopconstraintset">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SingleAnimationActivity" />
        <activity android:name=".ImageScaleActivity" />
        <activity android:name=".LayoutActivity" />
        <activity android:name=".AddButtonProblemActivity"></activity>
        <activity android:name=".Layout02Activity">

        </activity>
        <activity android:name=".apidemo.TransitionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>